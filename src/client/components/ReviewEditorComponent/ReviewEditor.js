import React from 'react';
import PropTypes from 'prop-types';


// Make sure that Avatar, Name, Input Text field with placeholder text and Button 
export const ReviewEditor= ({
  avatarUrl,
  fullName,
  inputText,
  placeHolderText,
  buttonStyle
})=>{
    return (
        <div>
            
        </div>
    )
};

// eslint-disable-next-line no-undef
ReviewInputComponent.propTypes = {
  avatarUrl: PropTypes.string.isRequired,
    fullName: PropTypes.string.isRequired,
  };

//   ProfileCardComponent.defaultProps = {
//     style: {},
//   };
  



