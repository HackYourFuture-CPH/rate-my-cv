import React from 'react';
import { CvcardList } from './CvcardList';

export default { title: 'cv card list search' };

export const CvcardListSearch = () => (
  <CvcardList title="All resumes 16 results" />
);
